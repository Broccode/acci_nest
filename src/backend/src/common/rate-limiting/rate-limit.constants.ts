/**
 * Rate limiting metadata key for number of allowed requests
 */
export const RATE_LIMIT_POINTS_KEY = 'rate-limit:points';

/**
 * Rate limiting metadata key for time window duration in seconds
 */
export const RATE_LIMIT_DURATION_KEY = 'rate-limit:duration';

/**
 * Rate limiting metadata key for Redis key prefix
 */
export const RATE_LIMIT_KEY_PREFIX_KEY = 'rate-limit:key-prefix'; 